import { test } from '@playwright/test';


test.describe('Article creation tests', () => {


    test.beforeEach(async ({ page }) => {

    });

    test('User can create an acrticle', async () => {

    });
});
